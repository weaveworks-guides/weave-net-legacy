    var fs = require('fs');
    var sshKeyToPEM = require('./');
    var path = require('path');

    var sshKey = fs.readFileSync(path.join(process.env.HOME, '.ssh/id_rsa.pub'), 'ascii');
    var key = sshKeyToPEM(sshKey);

    console.log(key);

